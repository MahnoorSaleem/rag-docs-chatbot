import { OpenAI } from '@llamaindex/openai';
import { ClientOptions } from 'groq-sdk';

declare class Groq extends OpenAI {
    constructor(init?: Omit<Partial<OpenAI>, "session"> & {
        additionalSessionOptions?: ClientOptions;
    });
    get supportToolCall(): boolean;
}
/**
 * Convenience function to create a new Groq instance.
 * @param init - Optional initialization parameters for the Groq instance.
 * @returns A new Groq instance.
 */
declare const groq: (init?: ConstructorParameters<typeof Groq>[0]) => Groq;

export { Groq, groq };
